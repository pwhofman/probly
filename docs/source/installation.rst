.. _installation:

Installation
============

`probly` is intended to work with **Python 3.12 and above**. Installation can be done via `uv` or
directly via `pip`:

.. code-block:: sh

   uv add probly

or

.. code-block:: sh

   pip install probly

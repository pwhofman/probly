.. _installation:

Installation
============

`probly` is intended to work with **Python 3.12 and above**. Installation can be done via `pip` and
or `uv`:

.. code-block:: sh

   pip install probly

or

.. code-block:: sh

   uv add probly

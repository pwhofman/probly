# @package method
name: "credal_relative_likelihood"
label: "CreRL"
params:
    num_members: 10
    reset_params: true
    tobias_value: 100.0
train:
    alpha: 1.0
    batch_check: false # determines whether likelihood constraint is checked after batch (if true) or epoch (if false)
active_learning:
    train:
        alpha: 0.8 # AL-only override of the relative-likelihood threshold; default 1.0 is too strict for small labeled pools

defaults:
  - comparison: ???
  - _self_

wandb:
  entity: "probly"
  project: "test"

seeds: [1, 2, 3]
top_k: 4
bins: 50
measure: default
decomposition: epistemic

show: false

cache:
  mode: read   # read | refresh | off

# null -> FIGURE_PATH; relative paths resolve from repo root.
# bar_ranked.py task=ood must have written
# bar_ood_near_<dataset>.ranking.json and bar_ood_far_<dataset>.ranking.json
# into the same directory.
save_path: null

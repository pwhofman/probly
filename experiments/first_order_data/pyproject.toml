[project]
name = "probly-first-order-data-experiments"
version = "0.0.0"
description = "Local environment for first-order data experiments."
requires-python = ">=3.13.0,<3.15"
dependencies = []

[tool.uv]
package = false
